# Pinned with lower + upper bounds. yfinance breaks often on Yahoo API
# changes, so the upper bound is important to avoid silent breakage.
#
# Local dev/test deps for the shared portfoy/ package. The Flask API has its
# own scoped api/requirements.txt (adds flask, used for the Vercel deploy).
yfinance>=1.4,<2.0
pandas>=3.0,<4.0
numpy>=2.4,<3.0
requests>=2.32,<3.0
defusedxml>=0.7,<1.0
